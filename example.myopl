

PRINT("asd")

FOR i = 1 TO n THEN
        IF i == 1 THEN PRINT(t1)
        
        IF i == 2 THEN PRINT(t2)
        
        nextTerm = t1 + t2;
        t1 = t2;
        t2 = nextTerm;
        PRINT(nextTerm)
END

int fib(int n)
{
    if (n <= 1)
        return n;
    return fib(n-1) + fib(n-2);
}
 
int main ()
{
    int n = 9;
    cout << fib(n);
    getchar();
    return 0;
}

